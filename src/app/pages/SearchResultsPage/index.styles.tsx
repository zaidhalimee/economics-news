import { css, Theme } from '@emotion/react';

const styles = {
  container: () =>
    css({
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
    }),
};

export default styles;
